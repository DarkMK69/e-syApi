🚨 Incident Management API
<div align="center">
https://img.shields.io/badge/Python-3.8+-blue.svg
https://img.shields.io/badge/FastAPI-0.104.1-green.svg
https://img.shields.io/badge/SQLite-3.0-lightgrey.svg
https://img.shields.io/badge/License-MIT-yellow.svg

API-сервис для учёта и управления инцидентами
Больше никаких потерянных сообщений о проблемах в чатах!

Особенности • Быстрый старт • Документация • API • Разработка

</div>
📖 О проекте
Сервис предназначен для централизованного учёта инцидентов от операторов и систем. Вместо того чтобы терять важные сообщения о проблемах (самокат не в сети, точка не отвечает, отчёт не выгрузился) в чатах Telegram, используйте этот API для структурированного управления инцидентами.

🎯 Решаемые проблемы
✅ Централизация - все инциденты в одном месте

✅ Отслеживание статусов - понятный workflow решения проблем

✅ Статистика - аналитика по источникам и статусам проблем

✅ Автоматизация - интеграция с системами мониторинга

✨ Особенности
🚀 Быстрый - построен на FastAPI с автоматической валидацией

📊 Статистика - встроенная аналитика по инцидентам

🔍 Фильтрация - гибкий поиск по статусам и источникам

📚 Документация - автоматическая генерация Swagger/ReDoc

🐍 Простота - чистый Python код с type hints

💾 Надёжность - SQLite с ORM SQLAlchemy

🛠 Технологический стек
Технология	Назначение
Python 3.8+	Основной язык программирования
FastAPI	Современный веб-фреймворк
SQLAlchemy	ORM для работы с базой данных
Pydantic	Валидация данных и сериализация
SQLite	Лёгкая файловая база данных
Uvicorn	ASGI сервер для production
📦 Установка и настройка
Предварительные требования
Python 3.8 или выше

pip (менеджер пакетов Python)

🚀 Быстрый старт
bash
# 1. Клонируйте репозиторий
git clone https://github.com/your-username/incident-api.git
cd incident-api

# 2. Создайте виртуальное окружение
python -m venv venv

# 3. Активируйте виртуальное окружение
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Заполните базу тестовыми данными
python populate_db.py

# 6. Запустите сервер
uvicorn main:app --reload
Сервис будет доступен по адресу: http://localhost:8000

📚 Документация
После запуска доступны автоматически сгенерированные документации:

📋 Swagger UI: http://localhost:8000/docs

📖 ReDoc: http://localhost:8000/redoc

🎯 Модель данных
Инцидент (Incident)
Поле	Тип	Обязательное	Описание
id	Integer	✅ Авто	Уникальный идентификатор
description	String	✅	Текст/описание проблемы
status	Enum	✅	Статус инцидента
source	Enum	✅	Источник инцидента
created_at	DateTime	✅ Авто	Время создания
📊 Статусы инцидентов
python
class IncidentStatus(str, enum.Enum):
    NEW = "new"           # 🆕 Новый
    IN_PROGRESS = "in_progress" # 🔄 В работе
    RESOLVED = "resolved"       # ✅ Решён
    CLOSED = "closed"           # 🔒 Закрыт
📮 Источники инцидентов
python
class IncidentSource(str, enum.Enum):
    OPERATOR = "operator"     # 👨‍💼 Оператор
    MONITORING = "monitoring" # 📡 Мониторинг
    PARTNER = "partner"       # 🤝 Партнёр
🔌 API Документация
1. 📝 Создать инцидент
POST /incidents/

http
POST /incidents/
Content-Type: application/json

{
  "description": "Самокат не в сети более 2 часов",
  "source": "operator"
}
Ответ:

json
{
  "id": 1,
  "description": "Самокат не в сети более 2 часов",
  "status": "new",
  "source": "operator",
  "created_at": "2024-01-15T10:30:00.000Z"
}
2. 📋 Получить список инцидентов
GET /incidents/

Параметры запроса:

status - фильтр по статусу (опционально)

source - фильтр по источнику (опционально)

Примеры:

bash
# Все инциденты
curl "http://localhost:8000/incidents/"

# Только новые инциденты
curl "http://localhost:8000/incidents/?status=new"

# Инциденты от мониторинга
curl "http://localhost:8000/incidents/?source=monitoring"

# Новые инциденты от операторов
curl "http://localhost:8000/incidents/?status=new&source=operator"
3. 🔍 Получить инцидент по ID
GET /incidents/{id}

http
GET /incidents/1
4. ✏️ Обновить статус инцидента
PATCH /incidents/{id}

http
PATCH /incidents/1
Content-Type: application/json

{
  "status": "in_progress"
}
Ошибки:

404 Not Found - если инцидент не найден

5. 🗑️ Удалить инцидент
DELETE /incidents/{id}

http
DELETE /incidents/1
6. 📊 Получить статистику
GET /stats/

Ответ:

json
{
  "total_incidents": 20,
  "status_distribution": {
    "new": 5,
    "in_progress": 8,
    "resolved": 4,
    "closed": 3
  },
  "source_distribution": {
    "operator": 7,
    "monitoring": 9,
    "partner": 4
  },
  "last_incident_date": "2024-01-15T14:20:00.000Z"
}
💻 Примеры использования
Создание инцидента через curl
bash
curl -X POST "http://localhost:8000/incidents/" \
     -H "Content-Type: application/json" \
     -d '{
       "description": "Точка выдачи не отвечает на запросы",
       "source": "monitoring"
     }'
Обновление статуса
bash
curl -X PATCH "http://localhost:8000/incidents/1" \
     -H "Content-Type: application/json" \
     -d '{"status": "resolved"}'
Интеграция с Python
python
import requests

# Создание инцидента
response = requests.post(
    "http://localhost:8000/incidents/",
    json={
        "description": "Сервер API недоступен",
        "source": "monitoring"
    }
)

# Получение инцидентов в работе
incidents = requests.get(
    "http://localhost:8000/incidents/",
    params={"status": "in_progress"}
).json()
🗄️ Структура проекта
text
incident-api/
├── 📁 app/
│   ├── main.py              # 🚀 Основное приложение FastAPI
│   ├── database.py          # 🗃️ Настройка базы данных
│   ├── models.py            # 🎯 Модели SQLAlchemy
│   ├── schemas.py           # 📋 Pydantic схемы
│   └── populate_db.py       # 🎲 Скрипт тестовых данных
├── requirements.txt         # 📦 Зависимости
├── README.md               # 📚 Документация
└── incidents.db            # 💾 База данных (создаётся автоматически)
🔧 Разработка
Добавление новых статусов
python
# В models.py
class IncidentStatus(str, enum.Enum):
    NEW = "new"
    IN_PROGRESS = "in_progress"
    RESOLVED = "resolved"
    CLOSED = "closed"
    REOPENED = "reopened"        # 🔄 Новый статус
    WAITING_INFO = "waiting_info" # ⏳ Ожидание информации
Добавление новых источников
python
# В models.py
class IncidentSource(str, enum.Enum):
    OPERATOR = "operator"
    MONITORING = "monitoring"
    PARTNER = "partner"
    CUSTOMER = "customer"        # 👥 Новый источник
    AUTOMATION = "automation"    # 🤖 Автоматическая система
Заполнение тестовыми данными
bash
python populate_db.py
Скрипт создаст 20 разнообразных инцидентов с реалистичными данными.

🚀 Production развертывание
Рекомендации для production
База данных: Замените SQLite на PostgreSQL

Аутентификация: Добавьте JWT токены

Документация: Отключите в production (DEBUG=False)

Сервер: Используйте Uvicorn с workers

Пример production конфигурации
bash
# Запуск с несколькими workers
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4

# Использование Gunicorn с Uvicorn workers
gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app
Переменные окружения
Создайте файл .env:

env
DATABASE_URL=postgresql://user:password@localhost/incident_db
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com
🧪 Тестирование
Тестирование через Swagger UI
Откройте http://localhost:8000/docs для интерактивного тестирования всех endpoints.

Примеры тестовых запросов
bash
# Health check
curl http://localhost:8000/

# Статистика
curl http://localhost:8000/stats/

# Создание тестового инцидента
curl -X POST "http://localhost:8000/incidents/" \
     -H "Content-Type: application/json" \
     -d '{"description": "Тестовый инцидент", "source": "operator"}'
🤝 Участие в разработке
Мы приветствуем вклад в развитие проекта!

Форкните репозиторий

Создайте feature ветку (git checkout -b feature/amazing-feature)

Закоммитьте изменения (git commit -m 'Add some amazing feature')

Запушьте в ветку (git push origin feature/amazing-feature)

Откройте Pull Request

🐛 Сообщение о багах
Используйте Issues для сообщения о багах и предложения новых функций.

📄 Лицензия
Этот проект распространяется под лицензией MIT. Смотрите файл LICENSE для подробностей.

👥 Авторы
Ваше Имя - Разработчик - YourGitHub

🙏 Благодарности
FastAPI сообществу за отличную документацию

SQLAlchemy за мощный ORM

Всем контрибьюторам проекта

<div align="center">
⭐ Не забудьте поставить звезду репозиторию если проект вам понравился!
Если у вас есть вопросы или предложения - создавайте Issue!

</div>